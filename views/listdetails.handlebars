<h2>{{list.name}}</h2>
<p>{{list.description}}</p>
<h3>Games in this List</h3>
<ul>
    {{#each list.games}}
    <li>{{this}} - <a href="/getgame/{{this}}">
            {{#if this.cover}}
            <img alt="{{this.name}} Poster" src="https:{{this.cover}}">
            {{else}}
            <img alt="{{this.name}} Poster" src="/public/no_image.jpeg">
            {{/if}}
            {{this.name}}
            View Game
        </a>
    </li>
    {{/each}}
</ul>

<h3>Add a Game</h3>
<form action="/gamelist/{{list._id}}/addGame" method="POST">
    <label for="gameId">Game ID:</label>
    <input type="text" id="gameId" name="gameId" required />
    <button type="submit">Add Game</button>
</form>

<a href="/gamelist">Back to Game Lists</a>